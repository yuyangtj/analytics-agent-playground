models:
  - name: metricflow_time_spine
    description: Required by MetricFlow -- a plain calendar table, not a business metric.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day

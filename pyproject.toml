[project]
name = "analytics-agent-playground"
version = "0.1.0"
description = "Synthetic e-commerce business + benchmark for testing analytics agents' correctness and data-limitation awareness"
requires-python = ">=3.10"
dependencies = [
    "duckdb>=1.0.0",
    "faker>=24.0.0",
    "numpy>=1.26.0",
    "pandas>=2.0.0",
    "pyyaml>=6.0",
    "anthropic>=0.40.0",
]
